import styled from 'styled-components';

const Header = styled.header`
    height: 60px;
    box-shadow: 0px 22px 26px -19px rgba(0, 0, 0, 0.75);
    background-color: #fff;
    display: flex;
    align-items: center;
    padding-left: 20px;
    padding-right: 20px;
    font-size: 1.4em;
    box-sizing: border-box;
    position: sticky;
    top: 0;
`;

export { Header };
